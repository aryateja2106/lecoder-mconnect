[package]
name = "lecoder-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LeCoder MConnect v3 wire protocol DTOs. Matches the iOS Swift contract byte-for-byte. No tokio, no rmux."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serde_json = "1.0"
