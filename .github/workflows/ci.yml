name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Biome check (lint + format)
        run: npm run check --workspace=lecoder-mconnect

      - name: Run type check
        run: npm run typecheck --workspace=lecoder-mconnect

      - name: Run tests (skip native module tests on Linux)
        run: npm run test --workspace=lecoder-mconnect
        env:
          SKIP_NATIVE_TESTS: 'true'

      - name: Build
        run: npm run build --workspace=lecoder-mconnect

  # Full test suite on macOS (including native module tests)
  test-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Rebuild native modules
        run: npm rebuild

      - name: Run full test suite
        run: npm run test --workspace=lecoder-mconnect
